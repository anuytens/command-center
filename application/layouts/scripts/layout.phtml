<!doctype html>
<html lang="fr" >

    <head>
        <meta charset="utf-8">
        
        <style>
          body {
            padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
          }
        </style>

        <?php echo $this->headMeta() ?>
        <?php echo $this->headTitle() ?>
        <?php echo $this->headLink() ?>
        <?php echo $this->headStyle() ?>
        <?php echo $this->headScript() ?>

        <!--<script src="/js/libs/modernizr-2.5.3.min.js"></script>-->
    </head>
    
    <body>
    
        <!-- Nav bar -->
        <div class="navbar navbar-fixed-top">
            <div class="navbar-inner">
                <div class="container">
                    <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </a>
                    <a class="brand" href="#">SDIS62:Command Center</a>
                    <div class="nav-collapse collapse">
                        <?php echo $this->navigation()->menu()->setMaxDepth(0)->setUlClass('nav')->renderMenu($this->general_nav); ?>
                        <?php echo $this->navigation()->menu()->setUlClass('nav pull-right')->setMaxDepth(-1)->renderMenu($this->user_nav); ?>
                    </div>
                </div>
            </div>
        </div>

        <!-- Container -->
        <div class="container">
            <!-- Notifications -->
            <?php if( $this->flashMessenger()->hasMessages() ): ?>
                <ul id="flashmessenger" class="unstyled" >
                <?php echo $this->flashMessenger()->output(); ?>
                </ul>
            <?php endif ?>
            
            <!-- breadcrumb -->
            <?php echo $this->navigation()->breadcrumbs()->setPartial(array('breadcrumbs.phtml', 'default')); ?>
            
            <!-- Content -->
            <?php echo $this->layout()->content ?>
        </div>

        <!-- javascript (fast load)-->
        <?php echo $this->inlineScript() ?>
        
    </body>
    
</html>